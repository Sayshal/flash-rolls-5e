<section
  class="form-content tab {{#if tab.active}} active{{/if}}" data-group="{{tab.group}}" data-tab="{{tab.id}}"
>
  <h4><i class="fas fa-globe"></i> {{localize "FLASH_ROLLS.ui.dialogs.worldSettingsLabel"}} <span class="toggle-hint">{{localize 'FLASH_ROLLS.ui.dialogs.toggleHints'}}</span></h4>
  <hr/>

  <div class="form-group">
    <label for="showMenuOnLoad">{{fields.showMenuOnLoad.label}}</label>
    <input type="checkbox" name="showMenuOnLoad" {{#if showMenuOnLoad}}checked{{/if}} />
    <p class="hint">{{fields.showMenuOnLoad.hint}}</p> 
  </div>

  <div class="form-group">
    <label for="compactMode">{{fields.compactMode.label}}</label>
    <input type="checkbox" name="compactMode" {{#if compactMode}}checked{{/if}} />
    <p class="hint">{{fields.compactMode.hint}}</p> 
  </div>

  <div class="form-group">
    <label for="menuLayout">{{fields.menuLayout.label}}</label>
    <select name="menuLayout" data-current-value="{{menuLayout}}">
      {{#each fields.menuLayout.choices}}
      <option value="{{@key}}">
        {{this}}
      </option>
      {{/each}}
    </select>
    <p class="hint">{{{fields.menuLayout.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="skipRollDialog">{{fields.skipRollDialog.label}}</label>
    <input type="checkbox" name="skipRollDialog" {{#if skipRollDialog}}checked{{/if}} />
    <p class="hint">{{fields.skipRollDialog.hint}}</p> 
  </div>

  <div class="form-group">
    <label for="skipRollDialogOption">{{fields.skipRollDialogOption.label}}</label>
    <select name="skipRollDialogOption" data-current-value="{{skipRollDialogOption}}">
      {{#each fields.skipRollDialogOption.choices}}
      <option value="{{@key}}">
        {{this}}
      </option>
      {{/each}}
    </select>
    <p class="hint">{{{fields.skipRollDialogOption.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="showOnlyPCsWithToken">{{fields.showOnlyPCsWithToken.label}}</label>
    <input type="checkbox" name="showOnlyPCsWithToken" {{#if showOnlyPCsWithToken}}checked{{/if}} />
    <p class="hint">{{{fields.showOnlyPCsWithToken.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="addMacrosToFolder">{{fields.addMacrosToFolder.label}}</label>
    <input type="checkbox" name="addMacrosToFolder" {{#if addMacrosToFolder}}checked{{/if}} />
    <p class="hint">{{{fields.addMacrosToFolder.hint}}}</p> 
  </div>
  
</section>